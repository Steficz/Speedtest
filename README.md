# Speedtest
### Projekt pro měření rychlosti internetu a ukládání do Google Sheet

## Jak spustit
1. Nainstalujte závislosti: `pip install -r requirements.txt`
2. Konfigurujte soubor `config/config.json`
3. Spusťte skript: `python3 src/main.py`
